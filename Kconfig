# WANTED engine — top-level configuration, opened by `just menuconfig`.
# Kconfig.target picks how to build it (build-system only); Kconfig.engine
# defines what the binary is, sourced directly by hosts that build it in.

mainmenu "WANTED Engine Configuration"

source "Kconfig.target"
source "Kconfig.engine"
